// Вставка элемента в двоичную кучу
function min_binary_heap_insert() {
  // Дерево (массив), корень - tree[0]
  var tree = [2, 4, 9, 10, 5, 20, 13, 11, 15, 7];
  // Какой элемент вставлять
  const x = 13;
  //const x = 5;
  //const x = 111;
  
  // TODO: алгоритм вставки элемента x в дерево tree
  // можно использовать console.log(...); для промежуточного вывода
  // без return
}
