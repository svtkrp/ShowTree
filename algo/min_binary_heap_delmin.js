// Удаление минимального элемента (корня) в двоичной куче
function min_binary_heap_delmin() {
  // Дерево (массив), корень - tree[0]
  var tree = [2, 4, 9, 10, 5, 20, 13, 11, 15, 7];
  
  // TODO: алгоритм удаления минимального элемента (корня) в дереве tree
  // можно использовать console.log(...); для промежуточного вывода
  // без return
}
